# Core dependencies for notebook review
nbformat>=5.0  # For reading and parsing notebook files
openai>=1.0  # For OpenAI-compatible API integration
tabulate>=0.9.0  # For ASCII table formatting
python-dotenv>=0.19.0  # For loading .env files in local development
pandas